---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-cnpg
spec:
  # NOTE: When upgrading, the following must be done:
  #
  # $ kubectl cnpg psql immich-cnpg -d app
  # > ALTER EXTENSION vchord UPDATE;
  # > REINDEX INDEX face_index;
  # > REINDEX INDEX clip_index;
  #
  # See: https://docs.immich.app/administration/postgres-standalone#updating-vectorchord
  imageName: ghcr.io/tensorchord/cloudnative-vectorchord:18.6-1.1.1@sha256:c7672bc011351a587cc292a94aac7eeebb593ed2e120077bd9c9944c9467db93
  primaryUpdateStrategy: unsupervised
  instances: 2
  storage:
    size: 10Gi
    storageClass: longhorn-cnpg
  bootstrap:
    recovery:
      source: garage-backups
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: immich-garage-store
  externalClusters:
    - name: garage-backups
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: immich-garage-store
          serverName: immich-cnpg-v2
  postgresql:
    shared_preload_libraries:
      - vchord.so
    parameters:
      shared_buffers: 256MB
  resources:
    requests:
      cpu: 50m
    limits:
      memory: 1Gi
  monitoring:
    enablePodMonitor: true
