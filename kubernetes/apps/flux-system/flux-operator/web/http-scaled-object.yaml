---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/http.keda.sh/httpscaledobject_v1alpha1.json
apiVersion: http.keda.sh/v1alpha1
kind: HTTPScaledObject
metadata:
  name: flux-web
spec:
  hosts:
    - flux.${CLUSTER_DOMAIN}
  scaleTargetRef:
    name: flux-web
    service: flux-web
    port: 9080
  scalingMetric:
    requestRate:
      granularity: 1s
  scaledownPeriod: 300
  replicas:
    min: 0
    max: 1
